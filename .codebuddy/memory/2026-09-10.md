
## 2026-09-10 修复非字母开头应用分组
- 扩展端 `js/app.js` 软件列表分组逻辑改为：非字母（数字/符号/中文）开头统一归入 `#` 组。
- 解决 1Password 等数字开头应用误生成 "1" 分组的问题。

## 2026-09-10 优化快捷键图例布局
- 图例（popup 底部 `.key-legend`）从 3 列等宽网格 + 等号，改为 flex 横排键帽 + 微分组。
- 每个图例项上下结构（键帽 28px 圆角按键 + 文字说明），去掉等号；Mac 三键（⌘⌥⌃）后加分组间距。
- 改动文件：`js/app.js`（renderKeyLegend 结构）+ `css/style.css`；网页端无此图例无需同步。commit b83b7a6 推送。
