# 2026-07-17 日志

## 完成事项
1. **Header tagline i18n 修复**：`Header.tsx` 硬编码 `Shortcut Search Engine` 改为走 i18n，中文模式显示「快捷键搜索引擎」（commit `a86436c`）。
2. **版本号升级至 1.1.0**：同步修改 4 处（manifest.json / package.json / js/app.js VERSION / popup.html 关于弹窗），用于首次上架 Chrome 商店（commit `ad8d749`）。
3. **自动部署**：两次改动均已 deploy 到 Cloudflare Pages（master.keyatlas.pages.dev）。
4. **长期记忆更新**：写入「版本号同步规则（4 处）」和「每次改动后必须 auto-deploy」两条规则到 MEMORY.md。
