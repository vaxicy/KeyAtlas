# 2026-09-10

## 设置面板 UI 调整
- 合并「关于 KeyAtlas」与「反馈问题」入口：反馈按钮移入 about 卡片，删除独立 feedback 块。
- 移除版本号显示（用户要求：每次升版本不好管理）。改动涉及 popup.html、js/app.js（移除 el.aboutVersion 与 VERSION 常量）、js/i18n.js（删除 aboutVersion 文案）、css/style.css（删除 .about-version 及悬空 CSS，反馈按钮改药丸样式）。
- web 端无 about/版本号组件，无需同步。

## 新增「当前系统」偏好
- 需求：设置侧边栏选择系统（auto/windows/mac/linux），选后点进软件快捷键列表与搜索结果默认优先显示对应系统键位。
- 实现：popup.html 主题块后加 #setOSPref 分段控件；storage.js 加 KEYS.os + getOS/setOS；app.js 加 resolveDisplayOS/pickOS，init 读取偏好设置 state.os（无记录则 detectOS 回退，保持原行为）；i18n.js 加 osAuto；css #setOSPref 允许换行。
- 渲染层已统一走 state.os/effectiveOS()，无需改 cardHTML/renderDetail。
- 后续调整：去掉「自动」选项，只保留 Windows/macOS/Linux；css 改为 flex-nowrap + 按钮 flex:1，强制三按钮单行均分；旧的存储值 "auto" 在 init 时归一化为具体系统。
- 再调整：OS 控件在 340px 弹窗内溢出，进一步把按钮改为字号 11px、内边距 5px 4px、居中、gap 2px，避免 "Linux" 被截断。
- 再调整：用户要求分段控件选中项文字颜色与未选中项（跟随系统）一致，把 .segmented button.active 的 color 从 var(--primary) 改为 var(--sub)。
- 再调整：用户反馈「深色」偏浅灰突兀，希望与「跟随系统」同色，将 .segmented button 与 .segmented button.active 的 color 统一改为 var(--text)。
- 修复 bug：主题分段按钮带 data-theme="dark|light|system" 属性，原 CSS `[data-theme="dark"]` 是全局属性选择器，导致「深色」按钮自身匹配到深色调色板（--text 变浅色）而文字不可见。已把选择器限定为 `:root[data-theme="dark"]`，调色板只作用于文档根。

## 网页端主题与扩展端统一
- 网页端去掉「跟随系统」主题选项，只保留浅色/深色。
- web/src/theme.ts：ThemeMode 改为 'light' | 'dark'，默认 light，支持 URL ?theme= 参数，移除系统监听。
- web/src/components/Header.tsx：主题切换改为浅色 ↔ 深色二态。
- web/src/i18n.ts：删除 themeSystem 文案。
- web/index.html：内联脚本优先读取 URL ?theme= 并写入 localStorage，默认浅色。
- js/app.js：扩展点击「打开网页版」时带上 &theme={light|dark}，解析 currentTheme 到实际深浅色。
- web npm run build 通过。
