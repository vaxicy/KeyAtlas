# 2026-07-14 工作日志

## 第二十轮补充：数据逐批官方核实（每批 3 个）

用户要求：对「缺官方文档/基于整理版或 AI 知识库」的 12 个文件，分三批每批 3 个，逐批核实修正。

### 第一批（mastergo / invision / davinci）
- ⚠️ 抓取受阻：MasterGo 官方帮助页、InVision shortcuts.design、DaVinci pixflow 速查表均 403/fetch failed；defkey、CSDN 镜像也 403/failed。无法逐条抓官方源。
- **davinci.json**：修正 41 条 Mac 键位记法，从单词记法（Cmd/Shift/Option）统一为项目约定符号（⌘ ⇧ ⌥），Windows 的 `+` 补空格成 `Ctrl + X`。键位本身（Shift+1 切页面、F9 插入、M 标记、Ctrl+Shift+L 调色 Lift 轮等）为 Resolve 标准键，高把握正确，未改键。已提交 `e671910`。
- **mastergo.json**：Mac 已用 ⌘ 符号、Windows 已用 `Ctrl + X` 带空格，记法已一致；键位（V/R/O/L/P/T/H/Z、Ctrl+G 编组、⌘⌥K 组件、Shift+1 适应屏幕等）与 Figma 标准约定吻合，高把握正确。**未改动**。
- **invision.json**：同上，记法已一致；键位为标准设计工具约定。**未改动**。
- 结论：第一批实际改动仅 DaVinci 记法规范化；MasterGo/InVision 经知识核对为正确且记法已统一，待官方源恢复后再逐条抽检（重点疑点：InVision `Bring to front`=Ctrl+Shift+]、`davinci-snippermode`=A 等）。

### 待办（剩余 9 个，分三批）
第二批：figma / firefox / git
第三批：bilibili / abstract / pitch
第四批：lucidchart / marvel / lanhu
（web 抓取恢复后可优先补 MasterGo/InVision 官方抽检）

### 第一批补充（mastergo/invision 截图对照修复）
- **mastergo.json**：用户提供了官方快捷键大全截图，对照后补充 14 条（剪切/全选/粗体斜体下划线/文本对齐/画布对齐(6条)/水平垂直分布/保存/新建标签页），26→40 条。已提交 `4130cd1`。
- **invision.json**：用户提供了 InVision Studio 官方截图（Screen navigation + Building prototype），发现当前数据完全缺少 Studio 原型模式专属快捷键。追加 8 条（屏幕导航←→/高亮热区Shift/插入屏幕T/预览P/构建B/评论C/检查I/历史H），22→30 条。已提交 `4130cd1`。

### 第二批（figma / firefox / git）
- **figma.json**：68 条，键位格式已符合项目约定（无需改动）。JSON 格式规范化提交 `569d90f`。
- **firefox.json**：
  - 修复 bug：`edge-hard-refresh` ID → `firefox-hard-refresh`（从 edge 搬来的残留错误 ID）
  - 删除重复条目：`firefox-private-browsing` 与 `firefox-private` 功能完全重复（均 Ctrl+Shift+P）
  - 统一键位格式（47 条全部补空格）
  - 48→47 条。已提交 `e665e49`。
- **git.json**：34 条命令行命令，内容正确无需改动。

### 第三批（bilibili / abstract / pitch）
- **bilibili.json**：27 条，修复键位格式（`Shift+1`→`Shift + 1`、`⌘+Shift+C`→`⌘ ⇧ C` 等全部补空格）。已提交 `d4a3006`。
- **abstract.json**：14 条 Mac 专属，格式正确无需改动。
- **pitch.json**：17 条，格式正确无需改动。

### 第四批（lucidchart / marvel / lanhu）
- **lucidchart.json**：21 条，修复 zoom-in 的 `Ctrl + +`/`⌘ +` 格式歧义 → `Ctrl + =`/`⌘ =`。已提交 `4e1fab4`。
- **marvel.json**：15 条，格式正确无需改动。
- **lanhu.json**：15 条，格式正确无需改动。

### ✅ 全部四批完成
共处理 11 个文件（mastergo/invision/figma/firefox/git/bilibili/abstract/pitch/lucidchart/marvel/lanhu），均已完成修复并提交。

### 全局键位格式规范化（关键收尾）
- 用户反馈「任务显示未完成」：根因是前面 4 批只规范了 11 个指定文件，其余 **49 个文件仍有 3672 处旧格式**（`Ctrl+N` 无空格、`⌘+N` 带 `+` 号），与项目约定不一致。
- 规范约定（取自已规范的 davinci/figma）：
  - **Windows/Linux**：`Ctrl + Shift + A`（修饰键用 ` + ` 连接；单键 `V`/`F9`/`Space` 不变）
  - **Mac**：`⌘ ⇧ ⌥` 符号用空格连接、不用 `+`；`Cmd/Command→⌘`、`Option/Opt/Alt→⌥`、`Shift→⇧`、`Ctrl/Control→⌃`
  - 保留修饰键原始顺序、`/` 多快捷键分隔符、中文（如 `⌥ 点击`）、`—`/`无` 占位符
- 写幂等脚本 `scripts/_normalize_keys.js` 跑全 60 文件：**3780 处键位规范化，覆盖 52 文件**（8 个本就规范未动）。
- 校验 0 error / 0 warning，残留 `+` 格式问题 0。已提交 `89cae8b`，临时脚本已删除。

### Firefox + Figma 截图对照修正（commit a80f3b4）
- 用户指出之前把 Firefox 和 Figma 的截图**搞反了**（Firefox 截图被当成 Figma 处理）。
- 重新正确对照后修正：
  - **firefox.json（3 处）**：
    - `firefox-style-editor`: `Ctrl+Shift+M` → `Ctrl+Shift+Y`（与 responsive 冲突）
    - `firefox-zoom-in` mac: 残缺 `⌘` → `⌘ +`
    - `firefox-home` mac: `⌘ Shift Home` → `⌘ ⇧ Home`（不规范→符号化）
  - **figma.json（16 处）**：
    - `fig-align-bottom` 与 detach-instance 冲突 `Ctrl+Alt+B`，改为 `—`（无默认快捷键）
    - 15 处 mac 字段 `Option`→`⌥`、`Shift`→`⇧`、`⌘ Ctrl`→`⌘ ⌥` 符号规范化
    - `fig-view-zoom-in` mac 补全、`fig-object-mask` mac 修正混写
- 校验 0 error / 0 warning，键位冲突 0。

### 第二批未核实文件对照修正（commit e4d02e7）
用户提供了 Google Sheets（2张长截图）、飞书文档+表格（2张截图）：
- **googlesheets.json（56 条）**：与官方速查表对照完整，0 冲突 0 修改
- **googleslides.json（78 条）**：修正 2 处上下文冲突——to-front/to-back 与 move-begin/end 共用 Ctrl+Shift+↑↓，改为无默认键位
- **feishu.json（43→67 条，+24）**：新增文档编辑器快捷键 17 条（bold/italic/heading/link/code/divider/list/indent/undo/redo 等）+ 表格编辑器 4 条；zoom-in-window 改为 Ctrl+=/++ 避免冲突。剩余 Ctrl+K/F/B/I 等为跨上下文冲突（主App vs 文档 vs 表格），属正常
- 来源：Google Sheets 官方速查表 + 飞书帮助中心（在文档中使用快捷键 / 分享表格使用快捷键）
- 剩余未核实：20 个文件

### 第三批 Sketch 对照修正（commit 8940bf8）
用户提供了 Sketch 官方帮助中心快捷键一览页截图（2张 + 1张大图 sketch.jpg 已删）：
- **sketch.json（66→70 条，+4）**：修复 4 处键位错误（command-bar/zoom-in/increase-font/save-version），新增 cut/save-as/replace/remove-from-artboard
- 来源：Sketch 官方帮助中心「快捷键一览」页截图
- sketch.jpg 已删除
### 第四批 Framer/Canva/Miro/Mural 对照核实（commit d70f342）
用户提供了 6 张截图：Framer（1张）、Canva（Mac+Win 2张）、Miro（1张）、Mural（Win+Mac 2张）：
- **framer.json（67→68 条，+1）**：新增 reply-comment(P)，其余与官方 Keyboard Shortcuts 页面吻合
- **canva.json（40 条）**：与官方 Mac+Win 双平台截图完全吻合，无需修改
- **miro.json（31 条）**：与官方 Shortcuts and hotkeys Win/Mac 对照表吻合，无需修改
- **mural.json（35 条）**：与官方 Keyboard Shortcuts Win/Mac 截图完全吻合，无需修改
- 来源：各软件官方帮助中心快捷键页面
- 剩余未核实：13 个文件

### UI 功能：软件详情页新增 Mac/Windows 筛选（app.js）
用户要求「每个软件如果有 mac 或 windows 两个端支持按 mac 或 windows 筛选」。
- 方案：最小侵入，新增 `state.appFilterOS`（默认 all），仅在 Categories → app 下钻视图生效
- 做法：在 app 分支渲染时，先按 `state.appFilterOS` 过滤 items 再传给 `listHTML`（不改其签名，搜索页逻辑零改动）
- 新增 `appOsFilterHTML()` 分段控件（全部/Windows🪟/Mac🍎），复用 `.os-filter.segmented` 样式，用独立 `data-apposfilter` 属性避免与搜索页 `data-osfilter` 冲突
- 在 gotoApp / setTab / catView / drill / back 各导航入口重置 `appFilterOS="all"`
- i18n 复用已有 `all`/`os_windows`/`os_mac`/`filterLabel`，无硬编码
- 校验：node --check 通过，0 lint

### UI 功能迭代：app 级 OS 筛选改为"智能出现"简化版
用户反馈原"全部/Win/Mac 三选"筛选框没用（通用条目两平台都有，筛选变化小）。
改为：
- 新增 `appSupportedOS(appId)` 计算软件实际支持平台集合
- 单平台软件（只有 Win 或只有 Mac）→ 不显示筛选框，直接展示该套键位
- 跨平台软件 → 只显示实际支持的平台按钮（**去掉"全部"选项**），默认 `state.appFilterOS = state.os`（本机系统预选），可切另一平台
- 可选增强：筛选后 `state.suppressOsTag=true` 隐藏卡片冗余平台标签（render 开头重置）
- 仍不扰乱搜索页逻辑；node --check 通过，0 lint
