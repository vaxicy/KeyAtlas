# 2026-07-12

## KeyAtlas 谷歌扩展搭建（从 PRD 从零构建）
- 按 `KeyAtlas_MVP_PRD.md` 搭建了完整 Manifest V3 Chrome 扩展（快捷键搜索引擎）。
- 技术栈：Vanilla JS + HTML/CSS，无后端无登录，JSON 驱动数据。
- 目录结构：
  - `manifest.json`（MV3，去掉了 default_locale 避免强制 _locales）
  - `popup.html` + `css/style.css`（现代简约、圆角卡片、软阴影、深/浅主题、宽 380px）
  - `js/i18n.js`（双语 zh/en，语言优先级 URL ?lang= > localStorage > navigator.language）
  - `js/storage.js`（chrome.storage.local + localStorage 回退，收藏/最近50/主题）
  - `js/data.js`（加载并索引所有 JSON）
  - `js/search.js`（加权模糊搜索，name/desc/keywords/appName/keys 双语）
  - `js/app.js`（四 Tab：搜索/分类/收藏/最近，OS 自动检测，点击卡片复制按键）
  - `data/categories.json` `data/apps.json` + `data/shortcuts/*.json`（16 个应用/系统）
- 功能：搜索、分类下钻(分类→应用chip)、收藏、最近、OS 切换、语言切换全区域刷新、深浅主题、点击复制。
- README.md 含加载说明（chrome://extensions → 开发者模式 → 加载已解压）。
- lint 全通过。

## KeyAtlas popup UI 优化
- 目标：保留蓝白 SaaS 风、圆角/软阴影/深浅主题，提升 popup 空间利用率与搜索效率。
- 改动：
  - `popup.html`：Header 紧凑化；搜索框作核心入口；Tab 改**胶囊式**；齿轮按钮改为**右侧滑出设置层**（带遮罩）。
  - `css/style.css`：新增设置层、胶囊 Tab、开关 switch、空状态按钮、卡片分类标签等样式。
- 品牌定位保持：Shortcut Search Engine / 快捷键搜索引擎。

## KeyAtlas 首页交互重设计（Search First）
- 产品定位强化：快捷键搜索引擎，搜索优先，减少点击路径。
- `js/app.js`：
  - 搜索无查询时渲染**首页** `renderHomepage`：快速开始 + 热门软件胶囊 + 试试搜索示例词。
  - 结果卡片新规范：标题 + 软件标签 + 系统标签(仅软件类) + 描述。
  - 分类 Tab 降级为**三组二级浏览**：系统 / 软件 / 场景，用 catView/catDrill 管理。
  - 新增 gotoApp、syncTabs；点击路由支持 data-app/data-example/data-catview/data-drill/back。
- `js/search.js`：排序增加次级权重「软件 > 系统 > 应用名」；按键字符串规范化。
- `js/i18n.js`：占位符更新；新增 quickStart/popularApps/trySearch/grpSystem/grpSoftware/grpScenes 双语 key。
- `css/style.css`：新增首页分区、胶囊、卡片系统标签样式。

## KeyAtlas 移除高级筛选框（filterbar）
- 用户要求移除筛选框（遮挡视线，设置面板已有相同功能）。
- 从 popup.html/style.css/app.js 三处彻底删除 filterbar 全部代码（HTML/CSS/DOM引用/函数/事件监听）。

## KeyAtlas 移除同步账号功能
- 用户要求删除「同步设置」（跨设备同步/需登录账号），当前不需要账号系统。
- 改动 4 个文件：
  - `popup.html`：删除同步设置 setting-block（含 syncToggle 开关 UI）。
  - `js/app.js`：移除 el.syncToggle 引用、init 中 sync 状态读取与事件监听。
  - `js/storage.js`：移除 KEYS.sync、getSync()、setSync()。
  - `js/i18n.js`：移除中英双语 syncLabel/syncHint/syncOff/syncOn 四个 key。
- 设置面板剩余：语言 / 主题 / 当前系统 / 关于。JS 校验 + lint 零报错。

## 创建 user skill：store-asset-generator
- 用户要求把「商店截图/宣传图生成流程」做成 skill。已用 skill-creator 脚手架生成 **user 级** skill（跨项目通用）。
- 位置：`C:\Users\16704\.codebuddy\skills\store-asset-generator\`；打包产物 `D:\迅雷下载\vibe coding\store-asset-generator.zip`。
- 内容：
  - `SKILL.md`：触发场景、前置(Pillow + 像素字体)、强制工作流(生成→自预览→修复→重生成)、脚本作为模板使用说明。
  - `scripts/generate-store-screenshots.py`、`scripts/generate-store-promo.py`：从 color-picker 项目复制的 PIL 模板（1280×800 截图 + 440×280/1400×560 宣传图）。
  - `references/qa-checklist.md`：7 条视觉 QA 硬规则（字号随画布缩放、Banner 间距、偶数网格、底部安全线、区块防重叠、标题间距、自预览流程）+ 溢出/对齐/间距三检查。
  - `assets/PressStart2P-Regular.ttf`：像素字体。
- 注意：write_to_file 在中文 Windows 下会把文件写成 GBK，打包校验失败；已用 Python 转 UTF-8 修复。打包脚本在 Windows 需 `$env:PYTHONIOENCODING='utf-8'; $env:PYTHONUTF8=1` 才能跑（emoji 输出 + GBK 控制台）。description 不能用 `>-` 块标量（校验正则会把 `>` 当角括号报错），改用单行引号字符串。

## 把 store-asset-generator skill 整理成可发布本地仓库
- 用户要求把 skill 提交 GitHub，我帮他复制到独立目录并本地提交（GitHub 远程由用户自己操作）。
- 发布目录：`D:\迅雷下载\vibe coding\skills\store-asset-generator\`（已 `git init` + 首次提交 `d6fc413`，6 文件）。
- 新增 `README.md`（用途/结构/前置依赖/用法/安装）。
- 同步把「提交 skill 到 GitHub 的步骤」写入**永久 user rules**：`C:\Users\16704\.codebuddy\rules\publish-skill-to-github-RULE.md`（复制独立目录 → 写 README → git init 提交 → GitHub 建仓库关联 push → 可选 .gitignore；含 GBK/UTF-8、description 块标量等坑）。

## KeyAtlas 一轮优化（用户确认「全部按顺序来」）
按建议顺序执行 5 项，JS 校验 + JSON 校验 + lint 全通过：
1. **商店合规**：`manifest.json` 删除 `web_accessible_resources`（`data/*` 对 `<all_urls>` 开放没必要，数据用 `chrome.runtime.getURL` 在扩展内 fetch 即可）。
2. **数据质量**：
   - `apps.json` 给 chrome/vscode/figma/photoshop/excel 加 `"popular": true`；`app.js` 首页热门软件改为 `DataStore.apps.filter(a=>a.popular)` 数据驱动（去掉写死的 5 个 id）。
   - 场景视图过滤掉 `getShortcutsByCategory(c.id).length===0` 的空分类（video/ai 不再显示空磁贴）。
3. **死代码**：删 `state.category`/`state.app` 声明及赋值；删 `onContentClick` 里重复的 `[data-app]` 分支（第二个永远走不到）。
4. **键盘导航**（产品差异化）：新增 `onKeydown` + `getCards/setActiveCard/clearActiveCard`；↑/↓ 高亮 `.card-active`、Enter 复制高亮项、Esc 清空搜索或关设置；卡片点击抽成 `copyCard` 复用；`render()` 开头 `clearActiveCard()`；`style.css` 加 `.card-active` 高亮样式。
5. **版本常量**：`app.js` 新增 `const VERSION="1.0.0"`，about 版本号统一引用。
- 改动文件：manifest.json、data/apps.json、js/app.js、css/style.css。尚未 git 提交（待用户确认）。

## KeyAtlas 界面优化（用户确认「1」按 ①→③→②→④ 执行）
JS 校验 + JSON 校验 + lint 全通过：
1. **① 键盘提示 + 无结果空状态**：`popup.html` 搜索栏下方加 `.kbd-hint` 提示条（`↑↓ 选择 · Enter 复制 · Esc 清空`，data-i18n）；`i18n.js` 新增 `kbdHint`/`noResultsQuery` 双语 key；`renderSearch` 无结果时显示查询词（如 `未找到 "xxx" 的快捷键`）。`css/style.css` 加 `.kbd-hint` 样式。
2. **③ 对比度修复（a11y）**：浅色 `--sub` 由 `#667085`→`#5a6675`（约 4.0→5.4:1，达 AA）；新增 `--primary-strong:#3a55d9`（深蓝），激活 Tab / chip / 空状态按钮白字改深蓝底（白字对比度约 3.3→6.0:1）；新增 `:focus-visible` 焦点环（tab/icon-btn/pill/chip/segmented/star/link/empty-action/clear-btn），`:focus:not(:focus-visible){outline:none}` 避免鼠标双击出现轮廓。
3. **② 搜索命中高亮**：`app.js` 新增 `highlight(text,q)`（先 escapeHTML 再对匹配子串包 `<mark>`，正则转义特殊字符、ig 大小写不敏感）；`cardHTML(s,q)`/`listHTML(items,q)` 透传查询词，标题与描述命中处加粗染蓝；`css/style.css` 加 `.card-title mark/.card-desc mark` 样式（primary-soft 底 + primary-strong 字）。首页/收藏/最近不传 q → 不高亮。
4. **④ 设置面板满宽**：`.settings-panel` 宽 `320px`→`100%`（覆盖整 380px popup，更沉浸）。热门软件图标本就已有 `icon` 字段，pills 已渲染，无需改数据。
- 改动文件：popup.html、js/i18n.js、js/app.js、css/style.css。尚未 git 提交（待用户确认）。
