# 2026-07-17

## codex 网页端优化（已 git 提交）
用户让 codex 优化 KeyAtlas 网页端，review 后提交。
- `7724d98` feat(web): codex 网页端优化 — 静态SEO页/数据拆分/UI重构
- `1e20180` fix: keyatlas-privacy 作为普通目录纳入仓库（移除嵌套 git 仓库）

### codex 主要改动
- **数据拆分**：`web/public/data/all.json`（14万行单一文件）拆成 `web/public/data/shortcuts/*.json`（153 个应用各一文件）+ `apps.json` + `search.json`
- **静态 SEO 页**：新增 `web/scripts/gen-static-pages.ts`，build 时生成 153 应用 + 9 分类静态页（已接入 `package.json` build 脚本）
- **新增模块**：`seo.ts` / `webStorage.ts` / `ToastContext.ts` / `Highlight.tsx`（搜索高亮）
- **UI 重构**：`index.css` +373 行，Header/Home/AppDetail/SearchPage/AppCard/ShortcutCard 等大幅调整
- **新增资源**：`web/public/_headers`（缓存/安全头）、`keyatlas-privacy/privacy-policy.html`、`KeyAtlas-store-assets/`（商店素材 0.75MB）
- 构建验证通过（tsc + vite + gen-static-pages 均成功）

### 注意
- `keyatlas-privacy` 原本是嵌套 git 仓库（含 .git），首次 `git add` 被当 gitlink，隐私政策 HTML 未真正入库；已 `git rm --cached` + 删其 .git 后重新纳入。
- 此前 ShortcutCard 纯 CSS hover 修复仍保留在代码中（未被 codex 破坏）。
